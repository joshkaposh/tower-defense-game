import Tower from "./Tower";

export default class StandardTower extends Tower {
	constructor(id, cost, x, y, width, height, range, shotDelay) {
		super(id, cost, x, y, width, height, range, shotDelay);
	}
}
